import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Cash_Counter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
